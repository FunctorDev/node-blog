$(document).ready(function ($) {
  $('#camera_wrap_1').camera({
    loader: 'bar',
    pagination: false,
    thumbnails: false,
    hover: false,
    opacityOnGrid: false,
    imagePath: '../images/',
  });
});
